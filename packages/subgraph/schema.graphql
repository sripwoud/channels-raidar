enum ChannelStatus @entity {
  open
  closed
  settled
}

type Channel @entity {
  id: ID!
  openedAtBlock: BigInt!
  status: ChannelStatus!
  participant1: Bytes!
  participant2: Bytes!
  closedBy: Bytes
  closingNonce: BigInt
  settled_amount1: BigInt
  settled_amount2: BigInt
}

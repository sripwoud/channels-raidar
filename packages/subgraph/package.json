{
  "name": "subgraph",
  "version": "1.0.0",
  "scripts": {
    "auth": "graph auth https://api.thegraph.com/ $GRAPH_ACCESS_TOKEN",
    "build": "graph build",
    "codegen": "graph codegen --output-dir src/types/",
    "deploy": "graph deploy sripwoud/raiden-channels --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/ --access-token $GRAPH_ACCESS_TOKEN"
  }
}
